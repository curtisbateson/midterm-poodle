<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Poodle</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lobster|Roboto+Condensed">
  <link rel="stylesheet" href="/styles/index.css">
  <script src="/vendor/jquery-3.0.0.js" type="text/javascript"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.3/umd/popper.min.js" integrity="sha384-vFJXuSJphROIrBnz7yo7oB41mKfc8JzQZiCq4NCceLEaO4IHwicKwpJf9c9IpFgh" crossorigin="anonymous"></script>
  <script src="/scripts/app.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/js/bootstrap.min.js" integrity="sha384-alpBpkh1PFOepccYVYDB4do5UnbKysX5WZXm3XxPqe5iKTfUKjNkCk9SaVuEZflJ" crossorigin="anonymous"></script>
</head>
<body>
  <% include partials/_header %>
  <container class="container">
    <content class="row justify-content-center">
      <h3>Time to Poodle!</h3>
    </content>
    <content class="row justify-content-center">
      <h3><%= `${event.title} by ${organizer.name} - ${organizer.email}` %></h3>
    </content>
    <content class="row justify-content-center">
      <p><%= event.description %></p>
    </content>
    <content id="schedule_options" class="container">
      <div class="row justify-content-center">
        <table class="table">
            <thead>
              <tr>
                <th scope="col"></th>
                <% for (var option in schedule_options) { %>
                  <th scope="col"><%= `${schedule_options[option].date.slice(0, 10)} / ${schedule_options[option].time.slice(0, 5)}` %></th>
                <% } %>
              </tr>
            </thead>
            <tbody>
              <% for (var attendee in attendees) { %>
                <tr>
                  <th>
                    <p><%= `${attendees[attendee].name}`%> <br/> <%= `${attendees[attendee].email}`%> </p>
                  </th>
                  <% for (var option in schedule_options) { %>
                    <% var active = false; %>
                    <% for (var select in selected_options) { %>
                      <% if (selected_options[select].attendee_id === attendees[attendee].id && schedule_options[option].id === selected_options[select].schedule_option_id) { %>
                        <% active = true; %>
                      <% } %>
                    <% } %>
                    <% console.log() %>
                    <% if (active) { %>
                      <td>
                        <button type="button" class="btn btn-danger active" data-toggle="button" aria-pressed="true" autocomplete="off">
                          Poodle
                        </button>
                      </td>
                    <% } else { %>
                      <td>
                        <button type="button" class="btn btn-danger" data-toggle="button" aria-pressed="false" autocomplete="off">
                          Poodle
                        </button>
                      </td>
                    <% } %>
                  <% } %>
                </tr>
              <% } %>
              <tr>
                <th scope="row">
                    <form class='form' method='POST' action='/events/:id'>
                      <p class='form-group'>
                        <input class='form-control' type='text' name='name' placeholder="Name"/>
                      </p>
                      <p class='form-group'>
                        <input class='form-control' type='email' name='email' placeholder="Email"/>
                      </p>
                </th>
                  <% for (var option in schedule_options) { %>
                    <td>
                      <button type="button" class="btn btn-danger new-user" data-toggle="button" aria-pressed="false" autocomplete="off" data-option= <%= schedule_options[option].id %>>
                        Poodle
                      </button>
                    </td>
                  <% } %>
              </tr>
            </tbody>
          </table>
      </div>
    </content>
    <p class="row justify-content-center">
      <button class='btn btn-danger' type='submit'>Confirm Your Selections!</button>
    </form>
    </p>
  </container>
</body>
</html>
